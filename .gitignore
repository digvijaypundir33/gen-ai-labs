# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Node / React (practice-04/ui-react)
node_modules/
dist/

# macOS
.DS_Store

# Lambda build artifacts (regenerate from source with `zip function.zip lambda_function.py`)
*.zip

# Leftover AWS CLI outfile artifacts
response.json
hosted-config-version-response.json

# Pulled from an external repo, not ours to track (see interview-prep/README.md)
interview-prep/
